<%= form_for(:comment, url: post_comment_path(post_id: post.id), 
             :method => "patch") do |f| %>
  <%= f.text_area :content %>
  <%= f.hidden_field :post_id, :value => post.id %>

  <%= f.submit "Submit comment" %>
<% end %>